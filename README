				Unbootgen

Unbootgen parses Zynq boot images and can be used to display information from
contained headers or dump partition data.

Examples:
  Show all headers of an image:
    unbootgen show --all <bootimage>
  Show one header at offset:
    unbootgen show <bootimage> <offset>

  Dump partition data:
    unbootgen dump <bootimage> <offset> <output_file>
